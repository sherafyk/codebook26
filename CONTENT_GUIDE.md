# Content guide

Put markdown files anywhere under `content/`.

- `index.md` inside a folder becomes that folder's landing page.
- Any other markdown file becomes a page automatically.
- Use lowercase, hyphenated file names when possible.
